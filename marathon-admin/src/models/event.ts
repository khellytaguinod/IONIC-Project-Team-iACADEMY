export class Event {
  constructor(public name: string, public description: string, public date: string, public time: string, public location: string, public imgPath: string) {
    // this.startDate = new Date(this.startDate).toDateString();
  }
}